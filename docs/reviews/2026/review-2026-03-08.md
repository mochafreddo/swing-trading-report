# Agentic Technical Review — 2026-03-08

## 1) Executive summary (3-7 bullets)
- Scope: `git status`, `git diff --stat`, `git diff --cached --stat` 기준으로 작업 중 diff가 없어 whole-repo review로 진행했습니다. 문서([README](/Users/mochafreddo/GitHub/swing-trading-report/README.md), [PRD](/Users/mochafreddo/GitHub/swing-trading-report/docs/PRD.md), [ARCHITECTURE](/Users/mochafreddo/GitHub/swing-trading-report/docs/ARCHITECTURE.md), [STRATEGY](/Users/mochafreddo/GitHub/swing-trading-report/docs/STRATEGY.md))를 먼저 읽고 `sab scan/sell/entry`, `web`의 `auth/reports/holdings/run` 경계를 우선 추적했습니다.
- Blocker: `scan`이 후보별 raw close를 다시 네트워크로 조회하는 구조라, 종목 수가 늘수록 성능 문제가 아니라 신뢰성 문제로 번집니다.
- Blocker: 리포트 파일명과 `report_date`가 시장 세션 날짜가 아니라 실행 머신의 로컬 날짜를 사용해 US 흐름에서 artifact 의미가 흔들립니다.
- Blocker: `/api/run`과 server action 쪽 dispatch 경로에 중복 실행 제어가 없어 더블클릭/재시도만으로 동일 워크플로우가 중복 큐잉됩니다.
- Blocker: 기본 `/reports` 목록이 매 요청마다 `report_index` 전체 exact count를 강제해, 리포트가 쌓일수록 메인 화면이 먼저 느려집니다.
- Strength: Python/웹 테스트 밀도는 좋습니다. 실제로 `just quality`를 실행해 `543 passed`, `pnpm --dir web run test`를 실행해 `61 files / 340 tests passed`를 확인했습니다.
- Checklist coverage:
  - Checked: Correctness, Architecture, Security, Performance, Reliability/Observability, Testing/DX를 코드/문서/테스트 기준으로 점검.
  - Not checked: 실 KIS/Supabase/GitHub 연동, Actions 런타임 내부 concurrency 설정, 대규모 데이터셋 벤치마크, 실제 브라우저 E2E.

## 2) Findings (sorted)

### [MAJOR] `scan` 후처리가 후보별 추가 시세 호출을 발생시켜 규모가 커질수록 느려지고 실패 확률이 올라갑니다
- Evidence:
  - [`sab/market_data_service.py`](/Users/mochafreddo/GitHub/swing-trading-report/sab/market_data_service.py#L189) 에서 스캔 시작 시 전체 티커의 adjusted 캔들을 먼저 수집합니다.
  - [`sab/scan_evaluation.py`](/Users/mochafreddo/GitHub/swing-trading-report/sab/scan_evaluation.py#L308) 에서 후보별로 `_enrich_entry_reference_prices()`를 돌립니다.
  - [`sab/scan_evaluation.py`](/Users/mochafreddo/GitHub/swing-trading-report/sab/scan_evaluation.py#L62) 과 [`sab/scan_evaluation.py`](/Users/mochafreddo/GitHub/swing-trading-report/sab/scan_evaluation.py#L330) 에서 각 후보마다 raw close를 다시 KIS/PyKRX로 조회합니다.
- Impact:
  - 후보 수가 늘수록 네트워크 호출이 선형 증가합니다.
  - PRD의 “20~30개 종목 1~2분” 목표와 충돌하고, 마지막 후처리 단계에서 KIS rate limit이나 일시 장애에 더 취약해집니다.
  - 테스트는 통과해도 운영 신뢰성은 계속 나빠지는 구조라 실제 PR이면 merge-block 하겠습니다.
- Recommendation:
  - 최소 수정은 candidate 후처리 단계의 per-ticker network call 제거입니다.
  - 초기 market-data 수집 시 raw/adjusted를 함께 확보하거나, 적어도 `eval_date`의 raw close만 배치 조회해 runtime/cache에 보관하세요.
  - `_resolve_raw_entry_reference_close()`는 cache hit 전용으로 바꾸고 miss만 경고 처리하는 편이 안전합니다.
- Effort: M. Confidence: High.

### [MAJOR] 리포트 파일명과 `report_date`가 시장 세션 날짜가 아니라 호스트 로컬 날짜 기준이라 US 흐름에서 날짜 의미가 흔들립니다
- Evidence:
  - [`sab/report/time_label.py`](/Users/mochafreddo/GitHub/swing-trading-report/sab/report/time_label.py#L6) 은 `datetime.now().astimezone()`의 로컬 날짜를 사용합니다.
  - [`sab/report/markdown.py`](/Users/mochafreddo/GitHub/swing-trading-report/sab/report/markdown.py#L83) 와 [`sab/report/entry_report.py`](/Users/mochafreddo/GitHub/swing-trading-report/sab/report/entry_report.py#L55) 가 그 날짜를 그대로 파일명과 `report_date`에 씁니다.
  - 그런데 [`tests/test_entry_command.py`](/Users/mochafreddo/GitHub/swing-trading-report/tests/test_entry_command.py#L261) 은 `report_date=2026-02-26`인 입력으로 `signal_eval_date=2026-02-25`를 기대합니다.
- Impact:
  - KST에서 미국장 리포트를 만들면 artifact key, retention, “최신 리포트” 선택, 날짜 기반 추적이 실행 머신 타임존에 종속됩니다.
  - 문서는 시장 세션 기준 평가를 강조하는데 산출물 키만 로컬 날짜를 쓰면 장기 운영에서 계속 헷갈립니다.
  - 이것도 실제 PR이면 merge-block 하겠습니다.
- Recommendation:
  - `write_report()`와 `write_entry_report()`가 외부에서 받은 `artifact_date`를 우선 사용하도록 바꾸세요.
  - `generated_at`은 생성 시각, `report_date`/파일명은 “대표하는 시장 세션 날짜”로 분리하는 계약이 맞습니다.
- Effort: S-M. Confidence: High.

### [MAJOR] `run` 경로에 중복 실행 제어가 없어 더블클릭/재시도만으로 동일 워크플로우가 중복 dispatch 됩니다
- Evidence:
  - [`web/src/app/api/run/route.ts`](/Users/mochafreddo/GitHub/swing-trading-report/web/src/app/api/run/route.ts#L14) 와 [`web/src/app/actions/run.ts`](/Users/mochafreddo/GitHub/swing-trading-report/web/src/app/actions/run.ts#L25) 는 검증 후 바로 `dispatchWorkflow()`를 호출합니다.
  - [`web/src/lib/github-actions.ts`](/Users/mochafreddo/GitHub/swing-trading-report/web/src/lib/github-actions.ts#L91) 는 GitHub `workflow_dispatch`만 발행하고 요청 키나 락을 저장하지 않습니다.
  - 반면 같은 저장소의 add-buy는 멱등성 키를 강제합니다: [`web/src/app/api/holdings/[ticker]/add-buy/route.ts`](/Users/mochafreddo/GitHub/swing-trading-report/web/src/app/api/holdings/[ticker]/add-buy/route.ts#L83).
- Impact:
  - 사용자가 새로고침하거나 버튼을 두 번 누르는 것만으로 중복 실행이 생깁니다.
  - GitHub Actions 중복 비용, 리포트 suffix 증가, Storage/`report_index` 경합, 원인 분석 난이도가 같이 올라갑니다.
  - 지금은 1인 프로젝트여도 장기 운영에서 확실히 아픈 종류라 merge-block 하겠습니다.
- Recommendation:
  - 최소 diff는 `runtime_state` 기반 TTL 락입니다.
  - `(workflow, provider, universe)` 키로 `run_dispatch:*` 상태를 저장하고, 최근 N초 내 동일 키가 있으면 `202 already queued` 또는 `409 already running`을 반환하세요.
  - 가능하면 GitHub run id까지 저장해 UI가 최근 실행과 연결되게 하세요.
- Effort: M. Confidence: High.

### [MAJOR] 기본 리포트 목록이 매 요청마다 `count=exact`를 강제해 리포트가 쌓일수록 메인 화면이 먼저 느려집니다
- Evidence:
  - [`web/src/lib/reports-data.ts`](/Users/mochafreddo/GitHub/swing-trading-report/web/src/lib/reports-data.ts#L113) 의 no-query 경로가 `fetchReportIndexPage({ type, limit })`를 호출합니다.
  - [`web/src/lib/supabase-admin.ts`](/Users/mochafreddo/GitHub/swing-trading-report/web/src/lib/supabase-admin.ts#L555) 에서 `includeTotal` 기본값은 `true`입니다.
  - [`web/src/lib/supabase-admin.ts`](/Users/mochafreddo/GitHub/swing-trading-report/web/src/lib/supabase-admin.ts#L576) 이 `Prefer: count=exact`를 붙입니다.
- Impact:
  - 첫 화면은 limit 30만 보여주는데도 전체 row count를 계속 계산합니다.
  - 지금은 작은 데이터셋이라 괜찮아도, 월 단위로 리포트가 쌓이면 가장 자주 보는 `/reports`가 먼저 느려집니다.
  - 이것도 “나중에 갑자기 아픈” 구조라 merge-block 하겠습니다.
- Recommendation:
  - 기본 목록은 `includeTotal: false` + `lookahead: true`로 바꾸고 `hasMore` 기반 pagination만 노출하세요.
  - 정확한 total이 정말 필요하면 별도 요청으로 분리하는 편이 낫습니다.
- Effort: S. Confidence: High.

### [MINOR] 로그인 스로틀 기본 `degrade` 정책은 Supabase 장애 시 보안 제어를 조용히 프로세스 로컬 메모리로 약화합니다
- Evidence:
  - [`web/src/lib/login-throttle.ts`](/Users/mochafreddo/GitHub/swing-trading-report/web/src/lib/login-throttle.ts#L370) 부터 Supabase 호출 실패 시 `strict`가 아니면 메모리 스로틀로 폴백합니다.
  - [`web/src/lib/login-throttle.ts`](/Users/mochafreddo/GitHub/swing-trading-report/web/src/lib/login-throttle.ts#L327) 은 이 상황을 경고 로그만 남깁니다.
- Impact:
  - 현재 “로컬 단일 사용자” 가정에선 허용 가능하지만, 컨테이너 재시작이나 다중 인스턴스 운영으로 조금만 확장돼도 brute-force 보호가 예측 불가능해집니다.
  - 바로 blocker까지는 아니지만, 조용히 약해지는 보안 제어는 나중에 확실히 아픕니다.
- Recommendation:
  - 범위를 계속 localhost 전용으로 고정할 거면 문서에 이 약화를 더 명확히 적으세요.
  - 원격 운영 가능성을 조금이라도 열어둘 거면 기본값을 `strict`로 바꾸는 편이 맞습니다.
- Effort: S. Confidence: Medium.

## 3) Refactor roadmap

### Quick wins (1-2 days)
- `run` dispatch에 TTL 락 추가.
- `/api/reports` 기본 목록에서 `count=exact` 제거.
- 리포트 writer에 `artifact_date` 주입 가능하게 만들어 시장 세션 날짜와 생성 시각을 분리.

### Mid-term (1-2 weeks)
- `scan`의 raw close/reference 수집을 배치화하거나 cache화해서 후보별 네트워크 호출 제거.
- `run` 실행 상태와 최근 실행 이력을 `runtime_state` 또는 별도 테이블로 노출.
- `run` API와 server action의 dispatch 입력 조립 로직을 공용 함수로 통합.

### Long-term
- “로컬 단일 사용자 전용”을 계속 유지할지, 제한적 원격 운영까지 허용할지 아키텍처 문서에서 확정.
- Supabase/PostgREST 호출 계층을 도메인별 어댑터와 계약 테스트 중심으로 더 분리.
- 리포트/실행/상태를 통합한 운영 control plane 모델로 정리.

## 4) Delegable follow-ups
- `scan`의 raw reference close 조회를 배치/캐시 기반으로 바꾸는 패치와 회귀 테스트 작성.
- 리포트 writer에 `artifact_date` 계약을 추가하고 `scan`/`sell`/`entry` 호출부를 일괄 갱신.
- `run` 중복 실행 재현 테스트 추가.
- `/api/reports`에서 total 없는 pagination으로 바꾸고 UI가 `hasMore`만으로 동작하는지 검증.
- `login-throttle`의 `degrade`/`strict` 정책을 문서와 테스트에 명시.

## 5) Open questions / missing context
- 웹을 계속 `localhost` 단일 사용자 전용으로 고정할 계획인지, 아니면 제한적 원격 운영 가능성을 남길지 불분명합니다.
- `run` 중복 실행을 제품적으로 허용하려는 의도가 있는지 확인이 필요합니다. 허용이라면 UI와 문서에 그 사실을 명시해야 합니다.
- 전체 저장소를 전부 읽었다고 주장하지는 않습니다. 깊게 본 범위는 `sab/scan.py`, `sab/sell.py`, `sab/entry.py`, `sab/market_data_*`, `sab/report/*`, `web/src/app/api/**`, `web/src/lib/**`, 핵심 테스트와 문서입니다.
- 확인하지 않은 것:
  - GitHub Actions 워크플로우 내부의 concurrency 설정과 실제 배포 환경의 env 구성을 끝까지 검증하지는 않았습니다.
  - 성능은 코드 구조와 쿼리 형태 기준으로 판단했고, 프로덕션 규모 데이터셋 벤치마크는 수행하지 않았습니다.
