# AGENTS.md

이 저장소는 기존 구현을 정리하고 재구축을 시작하는 상태다.

- 새 요구사항을 기준으로 필요한 최소 구현부터 만든다.
- 이전 Git 이력은 참고 자료이며 현재 구현 요구사항으로 간주하지 않는다.
- 기존 Git 이력, 태그, 원격 저장소를 보존한다.
- `CLAUDE.md`는 이 파일을 참조한다. 공유 지침은 이 파일에서 관리하고 제자리에서 편집한다.

## Agent skills

### Issue tracker

이슈 조회·발행·수정에는 GitHub Issues를 사용한다. 작업 전에 `docs/agents/issue-tracker.md`를 읽는다.

### Triage labels

이슈를 분류하거나 라벨을 변경하기 전에 `docs/agents/triage-labels.md`의 기본 라벨 매핑을 읽는다.

### Domain docs

단일 도메인 구조를 사용한다. 코드와 도메인을 탐색하기 전에 `docs/agents/domain.md`를 읽는다.
